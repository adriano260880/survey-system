spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/survey

    redis:
      host: localhost
      port: 6379

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: INFO