apiVersion: 1

providers:
  - name: Survey System
    orgId: 1
    folder: Survey System
    type: file

    disableDeletion: false
    editable: true
    allowUiUpdates: true

    updateIntervalSeconds: 30

    options:
      path: /var/lib/grafana/dashboards